
#!/bin/sh
#
# Homebrew

if test ! $(which brew)
then
  echo " You should manually install homebrew, then press enter when done."
  open "http://brew.sh/"
  read
else
  echo " Homebrew is already installed"
fi

exit 0

